linear-issues:
  type: linear
  api_key: ${LINEAR_API_KEY}
  mode: issues
  poll_interval_s: 15
  active_states:
    - "Backlog"
    - "On Deck"
    - "In Progress"
    - "In Review"

linear-projects:
  type: linear
  api_key: ${LINEAR_API_KEY}
  mode: projects
  poll_interval_s: 30
  active_states:
    - "Planning"
